// require models
const db = require("../models");

module.exports = function (app) {

    // get last workout
    app.get("/api/workouts", (res, req) => {

    })

    // add exercise
    app.get("/api/workouts/:id", (res, req) => {
        
    })

    // create workout
    app.post("/api/workouts", (res, req) => {
        
    })

    // get workouts in range
    app.post("/api/workouts/range", (res, req) => {
        
    })

   


}
